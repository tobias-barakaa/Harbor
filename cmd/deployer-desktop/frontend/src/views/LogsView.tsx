export default function LogsView() {
  return (
    <div>
      <h1>Logs</h1>
      <p className="muted">Comes after Deployments — needs Wails runtime events, since deploy.Logs currently blocks.</p>
    </div>
  )
}
